services:
  - type: web
    name: rez-action-engine
    env: node
    region: mumbai
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: FEEDBACK_SERVICE_URL
        value: https://rez-feedback-service.onrender.com
