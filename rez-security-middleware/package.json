{
  "name": "@rez/security-middleware",
  "version": "1.0.0",
  "description": "Shared security middleware for all ReZ services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "security",
    "authentication",
    "rate-limiting",
    "middleware",
    "express"
  ],
  "author": "ReZ App",
  "license": "MIT",
  "peerDependencies": {
    "express": ">=4.0.0",
    "helmet": ">=7.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
